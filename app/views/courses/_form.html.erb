<%= form_for(course) do |f| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label>Your full name.</label>
    <%= f.text_field :name, :class => "form-control", :placeholder => "Enter your Name", :required => "true" %>
  </div>
 
  <div class="form-group">
    <label>Your active email.</label>
    <%= f.text_field :email, :class => "form-control", :placeholder => "Enter your Email", :required => "true"  %>
  </div>


  <div class="form-group">
   <p style="text-align: center;"><%= f.submit "Submit your details" , :class => "btn btn-success" %></p>
  </div>
<% end %>
